[package]
name = "fuzzy_match"
authors = ["Octomus Contributors <dev@octomus.dev>"]
version = "0.1.0"
edition = "2021"
description = "Performs fuzzy match with text against query"
publish.workspace = true
license.workspace = true

[dependencies]
fuzzy-matcher = "0.3.7"
