GITHUB_TOKEN will be auto-passed via env_passthrough.

Repo: AdrianTuci1/warp-new
Base branch: master

[bash]
git clone https://oauth2:${GITHUB_TOKEN}@github.com/AdrianTuci1/warp-new.git /tmp/repo
cd /tmp/repo
git checkout master
git checkout -b hermes/auth-pricing-billing-server
[/bash]

--- 

OBJECTIVE: Remove all cloud-dependent modules from the Warp Rust project (cargo workspace at /tmp/repo).

TASKS:

1. DELETE these directories and all their contents:
   - `app/src/auth/` (entire directory - auth_manager, login, SSO, web_handoff, etc.)
   - `app/src/pricing/` (entire directory - pricing mod.rs)
   - `app/src/billing/` (entire directory - billing mod.rs, shared_objects_creation_denied_modal)
   - `app/src/server/` (entire directory - graphql, cloud_objects, telemetry, server_api, sync_queue, experiments, iap, voice_transcriber, network_logging, etc.)
   - `app/src/cloud_object/` (entire directory - CloudObject trait, model, breadcrumbs, grab_edit_access_modal, toast_message)

2. EDIT `app/src/lib.rs`:
   REMOVE these module declarations (the lines and their content):
   - `mod auth;`
   - `mod billing;`
   - `mod pricing;`
   - `mod server;`
   - `mod cloud_object;`
   - `pub mod drive;`
   - `pub mod workspaces;`

   REMOVE these imports:
   - `use auth::auth_manager::AuthManager;`
   - `use auth::auth_state::{AuthState, AuthStateProvider};`
   - `use server::network_log_pane_manager::NetworkLogPaneManager;`
   - `use server::network_logging::NetworkLogModel;`
   - `use server::telemetry::context_provider::AppTelemetryContextProvider;`
   - `use server::voice_transcriber::ServerVoiceTranscriber;`
   - `use server::server_api::ServerApiProvider;`
   - `use server::cloud_objects::listener::Listener;`
   - `use server::cloud_objects::update_manager::UpdateManager;`
   - `use server::experiments::ServerExperiments;`
   - `use server::iap::IapManager;`
   - `use server::sync_queue::{QueueItem, SyncQueue};`
   - `pub use crate::server::telemetry::{AgentModeEntrypoint, AgentModeEntrypointSelectionType, TelemetryEvent};`
   - `use crate::server::telemetry::{AppStartupInfo, CloseTarget, PaletteSource, TelemetryCollector};`
   - `use crate::cloud_object::model::actions::{ObjectAction, ObjectActions};`
   - `use crate::cloud_object::model::persistence::CloudModel;`
   - `use crate::cloud_object::model::view::CloudViewModel;`
   - `use crate::drive::export::ExportManager;`
   - `use crate::drive::CloudObjectTypeAndId;`
   - `use crate::notebooks::CloudNotebook;`
   - `use crate::workspaces::team_tester::TeamTesterStatus;`
   - `use crate::workspaces::update_manager::TeamUpdateManager;`
   - `use crate::workspaces::user_profiles::UserProfiles;`
   - `use crate::workspaces::user_workspaces::{UserWorkspaces, UserWorkspacesEvent};`
   - `use crate::settings::cloud_preferences_syncer::initialize_cloud_preferences_syncer;`

   REMOVE all initialization code that references these modules (search for these patterns and remove the blocks):
   - `auth_state` variable and `AuthState::initialize`
   - `AuthStateProvider` singleton
   - `AuthManager` singleton  
   - `AppTelemetryContextProvider` singleton
   - `TelemetryCollector` singleton
   - `ServerApiProvider` and `server_api_provider` - remove the entire singleton registration
   - `UserWorkspaces` singleton registration and all related code
   - `CloudModel` singleton registration
   - `SyncQueue` singleton registration
   - `CloudViewModel` singleton registration
   - `UpdateManager` singleton registration
   - `Listener` singleton registration
   - `ServerExperiments` singleton registration
   - `ObjectActions` singleton registration
   - `ExportManager` singleton registration
   - `TelemetryEvent` references
   - `TeamTesterStatus` singleton registration
   - `TeamUpdateManager` singleton registration
   - `IapManager` singleton registration
   - `initialize_cloud_preferences_syncer` call
   - `ReferralThemeStatus` (OPTIONAL - keep or remove)

   Keep the code that refers to:
   - `ai::*` and `ai_assistant` (local AI should stay)
   - Terminal, pane_group, workspace, themes
   - Local settings, editor, code
   - MCP, skills, facts

   REMOVE these init calls in the init section:
   - `auth::init(ctx);`
   - `billing::shared_objects_creation_denied_modal::init(ctx);`
   - `drive::index::init(ctx);`
   - `drive::sharing::dialog::init(ctx);`
   - `workspaces` related code
   
   KEEP these init calls:
   - `ai::init(ctx);`
   - `workspace::init(ctx);`
   - All terminal, editor, pane_group, themes, etc.

   REMOVE the `user_is_logged_in` block and all its content (lines with `auth_state.is_logged_in()`, `on_first_frame_drawn`, etc.)
   KEEP the else branch (LoggedOutStartup).

   REMOVE the `ChangelogModel` singleton.

3. EDIT `app/Cargo.toml`:
   Search for dependencies that are only used by the removed modules and comment them out or remove them. At minimum:
   - Remove `cloud_object_client` dependency
   - Remove `cloud_object_models` dependency  
   - Remove `cloud_objects` dependency
   - Remove `warp_server_auth` dependency
   - Remove `warp_server_client` dependency
   - Remove `oauth2` dependency (if only used by auth)
   - Remove `cloud_object_persistence` dependency
   - Look for other deps that become unused

4. EDIT `app/src/notebooks/mod.rs`:
   - Remove or comment out imports from `server::`, `cloud_object::`, etc.
   - The `CloudNotebook` type is from cloud_objects - remove its usage
   - Simplify NotebookManager to work without cloud sync

5. EDIT `app/src/workflows/mod.rs`:
   - Remove cloud-related workflow types
   - Remove WorkflowSource variants that reference cloud

6. DELETE `app/src/server/` directories that may contain test fixtures needed for compilation (check if `app/src/server/block.rs` is referenced from other modules, e.g. network_logging types used elsewhere).

IMPORTANT NOTES:
- Do NOT delete `app/src/workspace/` directory - that's the main layout/view system (tab groups, panes, etc.)
- Do NOT delete `app/src/ai/` directory - local AI should stay
- Do NOT delete `app/src/drive/` - Agent 2 handles that
- Do NOT delete `app/src/workspaces/` - Agent 2 handles that
- Only delete directories listed in step 1
- The goal is that the project still COMPILES after this (or at least gets close). Fix what you can.
- If a file references a deleted module, try to remove the reference or replace with a stub.

After done:
```
git add -A
git commit -m "hermes/auth-pricing-billing-server: remove auth, pricing, billing, server, cloud_object modules"
git push origin hermes/auth-pricing-billing-server
```
