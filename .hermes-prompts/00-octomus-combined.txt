GITHUB_TOKEN will be auto-passed via env_passthrough.

Repo: AdrianTuci1/warp-new
Base branch: master

[bash]
git clone https://oauth2:${GITHUB_TOKEN}@github.com/AdrianTuci1/warp-new.git /tmp/repo
cd /tmp/repo
git checkout master
git checkout -b octomus/refactor-all
[/bash]

---

OBJECTIVE: Transform Warp into Octomus — a self-hosted terminal without Warp cloud dependencies.

IMPORTANT: Execute the tasks BELOW SEQUENTIALLY, in the order written. After all tasks, commit and push.

---

## TASK 1: Delete entire directories

Delete ALL contents of these directories (rm -rf):
- app/src/auth/
- app/src/pricing/
- app/src/billing/
- app/src/server/ (ENTIRE directory — graphql, cloud_objects, telemetry, server_api, sync_queue, experiments, iap, voice_transcriber, network_logging, block.rs, EVERYTHING)
- app/src/cloud_object/ (ENTIRE directory — model, breadcrumbs, grab_edit_access_modal, toast_message)
- app/src/drive/ (ENTIRE directory — panel, export, import, folders, sharing, index, workflows, EVERYTHING)
- app/src/workspaces/ (ENTIRE directory — team_tester, update_manager, user_profiles, user_workspaces, workspace.rs, team.rs, gql_convert)
- app/src/ai/cloud_environments/ (ENTIRE directory)
- app/src/ai/agent_sdk/ (ENTIRE directory)
- app/src/ai/cloud_agent_config/ (ENTIRE directory)
- app/src/ai/connected_self_hosted_workers.rs
- app/src/ai/connected_self_hosted_workers_tests.rs
- app/src/ai/cloud_agent_settings.rs
- app/src/ai/artifact_download.rs
- app/src/ai/artifact_download_tests.rs

---

## TASK 2: Edit app/src/lib.rs — Module declarations

Remove these lines from the module declarations section:
- `mod auth;`
- `mod billing;`
- `mod pricing;`
- `mod server;`
- `mod cloud_object;`
- `pub mod drive;`
- `pub mod workspaces;`
- `pub mod work flows;`

---

## TASK 3: Edit app/src/lib.rs — Remove all imports from deleted modules

Remove EVERY import that references these deleted modules. Search for and remove:

From `auth::`:
- `use auth::auth_manager::AuthManager;`
- `use auth::auth_state::{AuthState, AuthStateProvider};`

From `server::`:
- `use server::network_log_pane_manager::NetworkLogPaneManager;`
- `use server::network_logging::NetworkLogModel;`
- `use server::telemetry::context_provider::AppTelemetryContextProvider;`
- `use server::voice_transcriber::ServerVoiceTranscriber;`
- `use server::server_api::ServerApiProvider;`
- `use server::cloud_objects::listener::Listener;`
- `use server::cloud_objects::update_manager::UpdateManager;`
- `use server::experiments::ServerExperiments;`
- `use server::iap::IapManager;`
- `use server::sync_queue::{QueueItem, SyncQueue};`
- `pub use crate::server::telemetry::{AgentModeEntrypoint, AgentModeEntrypointSelectionType, TelemetryEvent};`
- `use crate::server::telemetry::{AppStartupInfo, CloseTarget, PaletteSource, TelemetryCollector};`

From `cloud_object::`:
- `use crate::cloud_object::model::actions::{ObjectAction, ObjectActions};`
- `use crate::cloud_object::model::persistence::CloudModel;`
- `use crate::cloud_object::model::view::CloudViewModel;`

From `drive::`:
- `use crate::drive::export::ExportManager;`
- `use crate::drive::CloudObjectTypeAndId;`

From `workspaces::`:
- `use crate::workspaces::team_tester::TeamTesterStatus;`
- `use crate::workspaces::update_manager::TeamUpdateManager;`
- `use crate::workspaces::user_profiles::UserProfiles;`
- `use crate::workspaces::user_workspaces::{UserWorkspaces, UserWorkspacesEvent};`

From `settings::`:
- `use crate::settings::cloud_preferences_syncer::initialize_cloud_preferences_syncer;`

Other:
- `use crate::notebooks::CloudNotebook;`
- `pub use crate::server::telemetry::*` (any remaining telemetry imports)
- `use crate::referral_theme_status::ReferralThemeStatus;`
- `use referral_theme_status::ReferralThemeStatus;`
- `use crate::changelog_model::ChangelogModel;`

---

## TASK 4: Edit app/src/lib.rs — Remove initialization blocks

In the `initialize_app` function, find and REMOVE these blocks. Be very careful to remove the ENTIRE block for each:

1. The `auth_state` variable: `let auth_state = Arc::new(AuthState::initialize(ctx, api_key));`
2. `iap_state` variable block (lines with `IapState`)
3. `server_api_provider` singleton registration block (the entire `ctx.add_singleton_model({...})` for ServerApiProvider)
4. `server_api` and `ai_client` variables derived from server_api_provider
5. `AuthStateProvider` singleton
6. `AppTelemetryContextProvider` singleton
7. `AuthManager` singleton
8. `ServerExperiments` singleton
9. `AIRequestUsageModel` singleton (it takes ai_client which is deleted)
10. `UserWorkspaces` singleton (takes team_client, workspace_client from server_api_provider)
11. `ApiKeyManager` singleton (references UserWorkspaces)
12. `ChangelogModel` singleton (it takes server_api)
13. Remove the ENTIRE block that references `NetworkLogModel` and `NetworkLogPaneManager`
14. Remove the `TelemetryCollector` singleton block
15. Remove the `CloudModel` singleton block
16. Remove the `SyncQueue` singleton block
17. Remove the ENTIRE block that creates `object_actions`, `cloud_objects`, `cached_workspaces`, etc. from sqlite_data
18. Remove `ObjectActions` singleton
19. Remove `TeamTesterStatus` singleton
20. Remove `TeamUpdateManager` singleton
21. Remove `UpdateManager` singleton
22. Remove `initialize_cloud_preferences_syncer` call
23. Remove `CloudViewModel` singleton
24. Remove `AIDocumentModel` singleton
25. Remove `Listener` singleton
26. Remove `IapManager` singleton
27. Remove `ExportManager` singleton
28. Remove `ReferralThemeStatus` singleton (and the model reference in GlobalResourceHandles)
29. Remove the `notebooks` variable that filters cloud_objects
30. Replace `NotebookManager::new(notebooks, ctx)` with `NotebookManager::new(vec![], ctx)`

REMOVE these init() calls:
- `auth::init(ctx);`
- `drive::index::init(ctx);`
- `drive::sharing::dialog::init(ctx);`
- `billing::shared_objects_creation_denied_modal::init(ctx);`

KEEP these init() calls:
- `ai::init(ctx);`
- `workspace::init(ctx);`
- All terminal, editor, pane_group, themes, etc.

REMOVE the entire `user_is_logged_in` block and everything inside it (the if/else that checks `auth_state.is_logged_in()`). Keep the else branch (LoggedOutStartup) logic.

---

## TASK 5: Edit app/src/ai/mod.rs

Remove these module declarations:
- `pub mod agent_sdk;`
- `pub mod cloud_agent_config;`
- `pub mod cloud_agent_settings;`
- `pub mod cloud_environments;`
- `pub mod connected_self_hosted_workers;`

Remove any imports from these modules.

---

## TASK 6: Rename "Warp" to "Octomus" in UI strings only

Search user-facing string literals in app/src/ and replace "Warp" with "Octomus" WHERE IT IS A UI-VISIBLE PRODUCT NAME. Do NOT change:
- Rust identifiers (struct names, enums, functions)
- File paths or module names
- Internal constant names
- Log/debug messages
- Cargo.toml package names
- Comments (unless they describe the product name to the user)

Files most likely to have UI strings:
- app/src/app_menus.rs
- app/src/root_view.rs
- app/src/command_palette.rs
- app/src/appearance.rs
- app/src/workspace/view/ (scan for "Warp" strings)
- app/src/settings/ (scan for "Warp" strings)
- app/src/terminal/view/ (scan for "Warp" strings)

---

## TASK 7: Clean up app/src/ai/llms.rs

Remove all predefined model configurations. The LLMPreferences should start empty — no preconfigured models. User adds their own provider/key. Replace the entire content with a minimal stub.

---

## TASK 8: app/Cargo.toml cleanup

Remove or comment out these dependencies (check they're not used elsewhere first):
- cloud_object_client
- cloud_object_models
- cloud_objects
- warp_server_auth
- warp_server_client
- cloud_object_persistence
- warp_graphql (if only used by server module)
- oauth2 (if only used by auth)

---

## TASK 9: Fix compilation errors

After all the above, run `cargo check 2>&1` and fix any compilation errors due to:
- Missing imports in files that referenced deleted modules
- Remove the `pub mod workflows;` line from lib.rs if you get errors about it
- Fix the `sqlite_data` destructuring to remove deleted fields
- Fix the `GlobalResourceHandles` struct — remove `referral_theme_status` field
- Fix any remaining references to deleted types

Keep running `cargo check` and fixing until it compiles (or as close as possible).

---

After done:
```
git add -A
git commit -m "octomus: remove cloud auth/server/drive/workspaces, rename Warp->Octomus, clean up AI cloud agents"
git push origin octomus/refactor-all
```
