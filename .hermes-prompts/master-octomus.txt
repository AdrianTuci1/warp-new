GITHUB_TOKEN auto-pasat via env_passthrough.
Repo: AdrianTuci1/warp-new

Ești orchestratorul Octomus. 1 sandbox Modal (16vCPU/32GB).
Clonează repo-ul, creează 6 worktree-uri, rulează 6 faze.
Fiecare fază face commit+push pe branch-ul ei.
La final, mergi totul și rulează cargo check până compilează.

=== SETUP ===
[bash]
git clone https://oauth2:${GITHUB_TOKEN}@github.com/AdrianTuci1/warp-new.git /tmp/repo
cd /tmp/repo
git worktree add /tmp/wt-1 -b octomus/agent-1-delete master
git worktree add /tmp/wt-2 -b octomus/agent-2-lib master
git worktree add /tmp/wt-3 -b octomus/agent-3-ai master
git worktree add /tmp/wt-4 -b octomus/agent-4-branding master
git worktree add /tmp/wt-5 -b octomus/agent-5-cargo master
git worktree add /tmp/wt-6 -b octomus/agent-6-simplify master
[/bash]
Raportează "✅ Setup complet — 6 worktrees gata. Încep PHASE 1."

=== PHASE 1 — DELETE ALL CLOUD DIRS & FILES (worktree /tmp/wt-1) ===
[bash]
cd /tmp/wt-1
# 22 directories to delete
rm -rf app/src/auth/
rm -rf app/src/pricing/
rm -rf app/src/billing/
rm -rf app/src/server/
rm -rf app/src/cloud_object/
rm -rf app/src/drive/
rm -rf app/src/workspaces/
rm -rf app/src/crash_reporting/
rm -rf app/src/remote_server/
rm -rf app/src/autoupdate/
rm -rf app/src/resource_center/
rm -rf app/src/experiments/
rm -rf app/src/ai/cloud_environments/
rm -rf app/src/ai/agent_sdk/
rm -rf app/src/ai/cloud_agent_config/
rm -rf app/src/ai/agent_management/
rm -rf app/src/terminal/shared_session/
rm -rf app/src/terminal/view/shared_session/
rm -rf app/src/settings_view/billing_and_usage/
rm -rf app/src/workspace/view/openwarp_launch_modal/
rm -rf app/src/workspace/view/orchestration_launch_modal/
rm -rf app/src/workspace/hoa_onboarding/
# 30+ individual files to delete
rm -f app/src/ai/connected_self_hosted_workers.rs
rm -f app/src/ai/connected_self_hosted_workers_tests.rs
rm -f app/src/ai/cloud_agent_settings.rs
rm -f app/src/ai/artifact_download.rs
rm -f app/src/ai/artifact_download_tests.rs
rm -f app/src/ai/request_usage_model.rs
rm -f app/src/ai/request_usage_model_tests.rs
rm -f app/src/ai/blocklist/telemetry_banner.rs
rm -f app/src/ai/agent_tips.rs
rm -f app/src/ai/auth_secret_types.rs
rm -f app/src/changelog_model.rs
rm -f app/src/reward_view.rs
rm -f app/src/referral_theme_status.rs
rm -f app/src/settings/changelog.rs
rm -f app/src/settings/cloud_preferences.rs
rm -f app/src/settings/cloud_preferences_syncer.rs
rm -f app/src/terminal/view/open_in_warp.rs
rm -f app/src/terminal/buy_credits_banner.rs
rm -f app/src/terminal/view/inline_banner/shared_sessions.rs
rm -f app/src/wasm_nux_dialog.rs
rm -f app/src/settings_view/admin_actions.rs
rm -f app/src/settings_view/teams_page.rs
rm -f app/src/settings_view/warp_drive_page.rs
rm -f app/src/settings_view/platform_page.rs
rm -f app/src/settings_view/platform/create_api_key_modal.rs
rm -f app/src/settings_view/referrals_page.rs
rm -f app/src/settings_view/billing_and_usage_page.rs
rm -f app/src/settings_view/billing_and_usage_page_v2.rs
rm -f app/src/workspace/view/free_tier_limit_hit_modal.rs
rm -f app/src/workspace/view/build_plan_migration_modal.rs
rm -f app/src/workspace/bonus_grant_notification_model.rs
git add -A
git commit -m "octomus: delete all cloud dirs + files (22 dirs, 30+ files)"
git push origin octomus/agent-1-delete
[/bash]
Raportează "✅ PHASE 1 done"

=== PHASE 2 — FIX lib.rs (worktree /tmp/wt-2) ===
Editează DOAR /tmp/wt-2/app/src/lib.rs.

1. Șterge MODULE DECLARATIONS:
mod auth; mod billing; mod pricing; mod server; mod cloud_object;
pub mod drive; pub mod workspaces;
#[cfg(feature = "crash_reporting")] mod crash_reporting; (tot blocul)
#[allow(dead_code)] mod remote_server;
mod autoupdate; mod changelog_model; mod reward_view;
mod referral_theme_status; mod resource_center; mod experiments;
mod wasm_nux_dialog;

2. Șterge TOATE IMPORTURILE care conțin:
auth::, billing::, pricing::, server::, cloud_object::, drive::, workspaces::,
crash_reporting::, remote_server::, autoupdate::, changelog_model::, reward_view::,
referral_theme_status::, resource_center::, experiments::, wasm_nux_dialog::,
cloud_preferences_syncer::, request_usage_model::, agent_tips::, auth_secret_types::,
open_in_warp::, buy_credits_banner::, teams_page::, warp_drive_page::, admin_actions::,
platform_page::, referrals_page::, billing_and_usage_page::, free_tier_limit_hit_modal::,
build_plan_migration_modal::, bonus_grant_notification_model::,
shared_session, shared_handlers, AgentModeEntrypoint, TelemetryEvent, OrchestrationLaunch

3. În initialize_app(), șterge TOATE singleton-ele:
AuthState, IapState, ServerApiProvider, AuthStateProvider, AppTelemetryContextProvider,
AuthManager, ServerExperiments, AIRequestUsageModel, UserWorkspaces, ApiKeyManager,
ChangelogModel, NetworkLogModel, TelemetryCollector, CloudModel, SyncQueue,
ObjectActions, TeamTesterStatus, TeamUpdateManager, UpdateManager, CloudViewModel,
AIDocumentModel, Listener, IapManager, ExportManager, ReferralThemeStatus,
ResourceCenter, Experiments, SharedSessionManager, RequestUsageModel,
OrchestrationViewer, OrchestrationLaunch

4. Șterge init() calls:
auth::init(ctx); drive::index::init(ctx); drive::sharing::dialog::init(ctx);
billing::shared_objects_creation_denied_modal::init(ctx);
experiments::init(ctx); resource_center::init(ctx);
PĂSTREAZĂ: ai::init(ctx);, workspace::init(ctx);, terminal, editor, pane_group, themes.

5. Șterge initialize_cloud_preferences_syncer call.
6. Înlocuiește `let notebooks = cloud_objects.iter().filter_map(...)` cu `NotebookManager::new(vec![], ctx)`
7. Șterge blocul `if user_is_logged_in {...} else {...}` — păstrează DOAR ramura else.
8. Șterge send_telemetry_*, telemetry::* references.
9. Simplifică sqlite_data — păstrează: app_state, workspace_language_servers, codebase_indices.
10. Folosește #[cfg_attr(test, expect(unused))] pentru variabile rămase.

[bash]
cd /tmp/wt-2
git add -A
git commit -m "octomus: clean up lib.rs — remove 25+ cloud module refs"
git push origin octomus/agent-2-lib
[/bash]
Raportează "✅ PHASE 2 done"

=== PHASE 3 — FIX AI MODULE (worktree /tmp/wt-3) ===
Editează DOAR /tmp/wt-3/app/src/ai/.

3a. /tmp/wt-3/app/src/ai/mod.rs — șterge module declarations:
pub mod agent_sdk; pub mod cloud_agent_config; pub mod cloud_agent_settings;
pub mod cloud_environments; pub mod connected_self_hosted_workers;
pub mod agent_management; (dacă există)
Șterge din init() orice apel către modulele șterse.

3b. /tmp/wt-3/app/src/ai/llms.rs — GOLEȘTE modelele predefinite.
Struct-ul LLMPreferences rămâne dar fără modele hardcodate.
User-ul setează Anthropic/OpenAI/custom endpoint în settings.

3c. /tmp/wt-3/app/src/ai/llms_tests.rs — șterge/comentează teste cu modele șterse.

3d. Verifică orice alt fișier din ai/ pentru import-uri din module șterse și șterge-le.

[bash]
cd /tmp/wt-3
git add -A
git commit -m "octomus: clean up ai — empty llms, remove cloud agent code"
git push origin octomus/agent-3-ai
[/bash]
Raportează "✅ PHASE 3 done"

=== PHASE 4 — BRANDING + BINARY RENAME (worktree /tmp/wt-4) ===

4a. UI STRINGS: Înlocuiește "Warp" → "Octomus" DOAR în string-uri UI.
NU atinge: crate names (warpui, warp_core), identificatori Rust, path-uri, feature flags.

[bash]
cd /tmp/wt-4
find app/src -name '*.rs' -exec sed -i 's/"Warp AI"/"Octomus AI"/g' {} +
find app/src -name '*.rs' -exec sed -i 's/"Warp Drive"/"Local Storage"/g' {} +
find app/src -name '*.rs' -exec sed -i 's/"About Warp"/"About Octomus"/g' {} +
find app/src -name '*.rs' -exec sed -i 's/"Warp Help"/"Octomus Help"/g' {} +
find app/src -name '*.rs' -exec sed -i 's/"Get Warp"/"Get Octomus"/g' {} +
[/bash]

Apoi citește manual fișierele cheie (app_menus.rs, root_view.rs, command_palette.rs,
settings/ rămase, onboarding, search_bar.rs, tips/ rămase, banner/ rămase)
și înlocuiește "Warp" rămas în string-uri UI cu "Octomus".

4b. BINARY RENAME — app/Cargo.toml:
- `name = "warp"` → `name = "octomus"`
- `default-run = "warp-oss"` → `default-run = "octomus-oss"`
- `[[bin]] name = "warp-oss"` → `name = "octomus-oss"`
- `[[bin]] name = "warp"` → `name = "octomus"`
- `[lib] name = "warp"` → `name = "octomus"`
- `bin/oss.rs` și `bin/local.rs` rămân ca path, nu le redenumi

4c. RENAME crate — crates/warp_cli/Cargo.toml:
- `name = "warp_cli"` → `name = "octomus_cli"`

4d. BUNDLE SCRIPT — script/macos/bundle:
Caută și înlocuiește TOATE referințele la binary/app names:
- `WARP_BIN="warp"` → `WARP_BIN="octomus"`
- `WARP_BIN="dev"` → rămâne (nu e prefixat warp)
- `WARP_BIN="preview"` → rămâne
- `WARP_BIN="stable"` → rămâne
- `WARP_BIN="warp-oss"` → `WARP_BIN="octomus-oss"`
- `WARP_APP_NAME="Warp"` → `WARP_APP_NAME="Octomus"`
- `WARP_APP_NAME="WarpDev"` → `WARP_APP_NAME="OctomusDev"`
- `WARP_APP_NAME="WarpLocal"` → `WARP_APP_NAME="OctomusLocal"`
- `WARP_APP_NAME="WarpPreview"` → `WARP_APP_NAME="OctomusPreview"`
- `WARP_APP_NAME="WarpOss"` → `WARP_APP_NAME="OctomusOss"`
- `BUNDLE_ID="dev.warp.Warp-..."` → `BUNDLE_ID="dev.octomus.Octomus-..."` etc.
- `WARP_SCHEME_NAME="warp"` → `WARP_SCHEME_NAME="octomus"`
- `WARP_SCHEME_NAME="warplocal"` → `WARP_SCHEME_NAME="octomuslocal"`
- `WARP_SCHEME_NAME="warpdev"` → `WARP_SCHEME_NAME="octomusdev"`
- `WARP_SCHEME_NAME="warppreview"` → `WARP_SCHEME_NAME="octomuspreview"`
- `WARP_SCHEME_NAME="warposs"` → `WARP_SCHEME_NAME="octomusoss"`
- DMG name: orice conține `Warp` → `Octomus`
- CLI wrapper: `bin/oz` e deja ok, dar `bin/warp-oss` → `bin/octomus-oss`
- NU schimba: `WARP_NOTARIZATION_PASSWORD`, `WARP_DEVELOPER_ID_CERT_PASSWORD`,
  `WARP_CODESIGN_KEYCHAIN_PASSWORD`, `WARP_PLIST_PATH` (env vars interne)

4e. LINUX PACKAGING — actualizează referințele:
- `resources/linux/debian/cli/control.template` — warp → octomus
- `resources/linux/debian/app/` — Warp → Octomus
- `resources/linux/arch/cli/PKGBUILD.template` — warp → octomus
- `resources/linux/rpm/cli/warp.spec.template` — warp.spec → octomus.spec, Warp → Octomus

[bash]
cd /tmp/wt-4
git add -A
git commit -m "octomus: rename Warp to Octomus — UI strings, binary, bundle, packages"
git push origin octomus/agent-4-branding
[/bash]
Raportează "✅ PHASE 4 done"

=== PHASE 5 — CARGO + SETTINGS + VOICE (worktree /tmp/wt-5) ===

5a. /tmp/wt-5/app/Cargo.toml — șterge dependențele:
cloud_object_client, cloud_object_models, cloud_objects, warp_server_auth,
warp_server_client, cloud_object_persistence, warp_graphql, oauth2
Șterge feature flags care referă module șterse.
Schimbă `warp_cli.workspace = true` → `octomus_cli.workspace = true` (crate-ul a fost redenumit in PHASE 4).

5b. /tmp/wt-5/app/src/settings/mod.rs — șterge module declarations:
pub mod cloud_preferences; pub mod cloud_preferences_syncer; pub mod changelog;

5c. /tmp/wt-5/app/src/settings/ai.rs — șterge referințe la cloud AI, server API.

5d. /tmp/wt-5/app/src/voice/mod.rs + transcriber.rs — șterge ServerVoiceTranscriber,
șterge import-urile din crate::server. Pune un stub dacă transcriber-ul e doar server-based.

5e. WORKSPACE ROOT — /tmp/wt-5/Cargo.toml (workspace root):
Schimbă `"crates/warp_cli"` → `"crates/octomus_cli"` în lista de workspace members.

5f. /tmp/wt-5/app/src/settings_view/mod.rs (dacă există) — șterge module declarations
pentru fișierele șterse în phase 1.

[bash]
cd /tmp/wt-5
git add -A
git commit -m "octomus: clean up Cargo.toml, settings, voice"
git push origin octomus/agent-5-cargo
[/bash]
Raportează "✅ PHASE 5 done"

=== PHASE 6 — SIMPLIFY REMAINING FILES (worktree /tmp/wt-6) ===

6a. /tmp/wt-6/app/src/notebooks/ — șterge import-uri din module șterse.
Șterge tipul CloudNotebook. Simplifică NotebookManager la local-only.

6b. /tmp/wt-6/app/src/workflows/ — șterge import-uri din module șterse.
Șterge WorkflowSource::Team, WorkflowSource::PersonalCloud. Păstrează doar local.

6c. /tmp/wt-6/app/src/util/links.rs — șterge URL-urile warp.dev.
Păstrează doar link-uri locale sau golește fișierul.

6d. /tmp/wt-6/app/src/app_menus.rs — șterge "Send Feedback", "Contact Support".
Șterge import-urile din module șterse.

6e. /tmp/wt-6/app/src/root_view.rs — șterge feedback/support actions.
Șterge import-urile din module șterse.

6f. /tmp/wt-6/app/src/settings_view/ai_page.rs — șterge:
- Warp API key management
- Cloud provider selection
- Usage/billing info
Păstrează doar: provider URL, model name, API key input.

6g. /tmp/wt-6/app/src/settings_view/mcp_servers/ — șterge referințe la marketplace.
Păstrează doar configurarea manuală.

6h. /tmp/wt-6/app/src/autoupdate/ — RESCRIE să verifice GitHub Releases
(https://github.com/AdrianTuci1/warp-new/releases) în loc de serverul Warp.

6i. Scanare finală: orice alt fișier cu import-uri din module șterse.

[bash]
cd /tmp/wt-6
git add -A
git commit -m "octomus: simplify notebooks, workflows, menus, settings — local only"
git push origin octomus/agent-6-simplify
[/bash]
Raportează "✅ PHASE 6 done"

=== FINAL: MERGE + CARGO CHECK ===
[bash]
cd /tmp/repo
git fetch origin
git checkout master
git checkout -b octomus/merged
for b in agent-1-delete agent-2-lib agent-3-ai agent-4-branding agent-5-cargo agent-6-simplify; do
  git merge "origin/octomus/$b" --no-edit || (git diff --name-only --diff-filter=U | xargs -r git checkout --theirs && git add . && git commit -m "merge $b")
done
[/bash]

Apoi:
[bash]
cd /tmp/repo
cargo check 2>&1 | head -120
[/bash]

Dacă sunt erori: citește fișierele cu probleme, editează, re-rulează cargo check.
Repetă până compilează (max 15 iterații).

La final:
[bash]
cd /tmp/repo
git add -A
git commit -m "octomus: merge all 6 agents + fix compilation"
git push origin octomus/merged
[/bash]
Raportează "🎉 OCTOMUS READY — octomus/merged pushed, cargo check trece!"

=== COMUNICARE ===
- După fiecare fază: "✅ PHASE X done"
- Dacă user-ul spune "skip X", "stop", "show status" — execută imediat
- Raportează numărul de erori rămase în timpul cargo check
